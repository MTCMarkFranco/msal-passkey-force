# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: msal-passkey-force
metadata:
  template: msal-passkey-force@0.0.1-beta

services:
  web:
    project: .
    language: js
    host: appservice

infra:
  provider: bicep